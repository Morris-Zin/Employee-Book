import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
    root: {
        justifyContent: 'center', 
        maxWidth: '100%', 
        maxHeight: '500px', 
        display: 'grid', 
        margin: '40px 0px', 
        placeItems: 'center'
    },
    goBackButton: {
        display: 'block',
        marginTop: '10px'
    }
  
  }));
  